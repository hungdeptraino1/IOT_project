nếu dùng vscode window, chạy python -m venv .venv ------>   .venv\Scripts\activate để có môi trường ảo
                ubuntu, python3 -m venv venv --> source venv/bin/activate           

chạy lệnh pip install -r requirements.txt trước để tải thư viện cần thiết trc khi chạy

app.py là stream video lên web

app(arduino).py là chạy local, điều khiển Servo

oldapp.py là code bản cũ (kệ)

creatdb là scrpit chạy 1 lần để tạo database (không chạy lại)